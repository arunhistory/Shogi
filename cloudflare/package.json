{
  "name": "shogi-cloudflare-runtime",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "stage:wasm": "node scripts/stage-wasm.mjs",
    "build": "npm run stage:wasm && wrangler deploy --dry-run --outdir dist",
    "e2e": "node scripts/e2e.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260827.1",
    "typescript": "5.9.2",
    "wrangler": "4.127.0",
    "ws": "8.21.3"
  }
}
